const somaConstraint = {
    soma: {
        type: 'number',
    }
};

const subtracaoConstraint = {
    subtracao: {
        type: 'number',
    }
};

const multiplicacaoConstraint = {
    multiplicacao: {
        type: 'number',
    }
};

const divisaoConstraint = {
    divisao: {
        type: 'number',
    }
};

const potenciaConstraint = {
    potencia: {
        type: 'number',
    }
};

module.exports = { somaConstraint };
module.exports = { subtracaoConstraint };
module.exports = { multiplicacaoConstraint };
module.exports = { divisaoConstraint };
module.exports = { potenciaConstraint };
